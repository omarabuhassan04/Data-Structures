# Data-Structures
A Java-based Task Management System demonstrating data structures. It uses a Linked List for tasks, a Stack for urgent tasks (LIFO), and a Queue for regular tasks (FIFO). Features include adding, completing, filtering, and sorting tasks using Merge and Selection Sort with performance timing.
